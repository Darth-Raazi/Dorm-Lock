import { Card } from 'react-native-paper';
import { View } from 'react-native';

export function CameraCard(props:any):JSX.Element{


return(
    <View style={props.containerStyle}>
        <Card> </Card>
    </View>
   ) 
}
